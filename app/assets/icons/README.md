# App Icon Swap

Drop your final icon assets here before submission:

- `app_icon.png` (1024x1024 recommended, square)
- `app_icon_foreground.png` (1024x1024 recommended, transparent background)

Then run:

```bash
flutter pub get
dart run flutter_launcher_icons
```

Rebuild the app after generating icons.
